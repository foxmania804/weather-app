# React + TypeScript + Vite

To run this project

- Install NodeJS and it on your system.
- Clone the repository and chane the directory to weather-app inside the cloned file
- Run `npm install`
- Run `npm run dev` tto run the code locally
